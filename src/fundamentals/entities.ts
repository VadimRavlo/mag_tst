import { Pool } from './pools/pool.entity';
import { Tick } from './ticks/tick.entity';
import { HandlingState } from './handling-state/handling-state.entity';

export const entities = [Pool, Tick, HandlingState];
